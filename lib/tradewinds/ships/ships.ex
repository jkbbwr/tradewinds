defmodule Tradewinds.Ships do
  import Ecto.Query, warn: false
  alias Tradewinds.Scope

  def buy_ship(%Scope{} = _scope, _company_id) do
  end

  def list_ships(%Scope{} = _scope, _company_id) do
  end

  def transit_ship(%Scope{} = _scope, _ship_id) do
  end
end
